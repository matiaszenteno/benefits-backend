# Configuración de Base de Datos (opcional si se usa Google Sheets)
DB_HOST=your-database-host
DB_PORT=5432
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# Configuración de Google Sheets (opcional)
# Si se configura esta URL, la lambda usará Google Sheets en lugar de la base de datos
# La URL debe ser de un Google Sheet público en formato CSV
# Ejemplo: https://docs.google.com/spreadsheets/d/SHEET_ID/export?format=csv&gid=0
GOOGLE_SHEETS_URL=

# Notas:
# - Si GOOGLE_SHEETS_URL está configurada, se usará como fuente principal de datos
# - Si GOOGLE_SHEETS_URL falla o no está configurada, se usará la base de datos como fallback
# - El Google Sheet debe tener las mismas columnas que la tabla benefits de la base de datos:
#   id, name, description, category, bank, provider, is_active, created_at, updated_at 